
capture:
  capture-time-seconds: 20

messages:
  captured: "&a%player% has captured the outpost!"
  contested: "&eThe outpost is being contested!"
  stopped: "&cThe outpost has been stopped."

bossbars:
  timer-title: "&6Outpost ends in &e%time%"
  capture-title: "&aCapturing outpost..."
